var VoronoiNode = {

}

var VoronoiTreemap = {
	
	// in: bounding polygon and node (and a depth?)
	// out: a list of polygons (maybe want the depth of the polygons as well)
	computeVoronoiTreemap = function(bounding_polygon, node, depth) {
		// call single level version on node
		// for each child of node, call recursively
	
	}

}